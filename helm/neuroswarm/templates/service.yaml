apiVersion: v1
kind: Service
metadata:
  name: {{ include "neuroswarm.fullname" . }}
  labels:
    {{- include "neuroswarm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "neuroswarm.selectorLabels" . | nindent 4 }}